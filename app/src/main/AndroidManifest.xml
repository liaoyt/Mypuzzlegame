<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell_pc.test">

    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".Login"
            android:label="Login">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        </activity>
        <activity
            android:name=".Activity01"
            android:label="@string/title_activity_01"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity02"
            android:label="@string/title_activity_02"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PuzzleActivity"
            android:label="testpuzzle" />
        <!--
            <activity android:name=".Login"
            android:label="Login"/>
        -->
        <activity
            android:name=".Register"
            android:label="Register"></activity>
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
